services:
  api:
    networks:
      - default
      - local-gateway
  web:
    networks:
      - default
      - local-gateway
  mail:
    networks:
      - default
      - local-gateway

networks:
  local-gateway:
    external: true
    name: local-gateway
